# Streamlit theme + server config for the hosted dashboard.
[theme]
primaryColor = "#1f6feb"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f3f6fb"
textColor = "#0d1117"
font = "sans serif"

[server]
headless = true
enableCORS = false

[browser]
gatherUsageStats = false
